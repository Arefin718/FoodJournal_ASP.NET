@model IEnumerable<DataLayer.Class.Admin>

@{
    ViewBag.Title = "List";
    Layout = "~/Views/Shared/adminheaderandfooter.cshtml";
}

<div class="container">
    <h2>List of Admin</h2>

    <p>
        @Html.ActionLink("Create New Admin", "Create")
    </p>
    <table class="table">
        <tr>
            <th>
               
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Email)
            </th>
          
           
            <th></th>
        </tr>

        @foreach (var item in Model)
        {
            <tr>
                <td>
                    <img style="height:100px; width:100px;" class="img-circle" alt="@item.Name image" src="@item.Image" />
                   
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Name)
                </td>
                <td>
                    @Html.DisplayFor(modelItem => item.Email)
                </td>
               
               @*
    <td>
        @Html.ActionLink("Edit", "Edit", new { id = item.AdminID }) |
        @Html.ActionLink("Details", "Details", new { id = item.AdminID }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.AdminID })
    </td>*@
            </tr>
        }

    </table>
</div>